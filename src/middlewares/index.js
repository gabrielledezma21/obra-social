const genericMiddleware = require('./genericMiddleware');
const prestadorMiddleware = require('./prestadorMiddleware');
const agendaMiddleware = require('./agendaMiddleware');

module.exports = {
  genericMiddleware,
  prestadorMiddleware,
  agendaMiddleware
}